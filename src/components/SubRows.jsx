import React, { useState } from 'react'
import { valueTypes } from '../common/common'

export default function SubRows({subMenus}) {
    const [editField, setEditField] = useState(false);
    return (
        <div>
           
        </div>
    )
}
